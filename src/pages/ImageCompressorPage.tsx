import React from 'react';

interface ImageCompressorPageProps {
  isMobile: boolean;
}

export const ImageCompressorPage: React.FC<ImageCompressorPageProps> = ({ isMobile }) => {
    return (
        <div>
            <h1>Compress Images</h1>
            {/* Implement your component here */}
        </div>
    );
}
